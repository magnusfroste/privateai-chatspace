# =============================================================================
# Private AI Chatspace - Frontend Environment
# =============================================================================
#
# USAGE:
#   1. Copy this file: cp .env.example .env
#   2. Run: npm run dev
#
# NOTE: These are only used during local development (npm run dev).
#       In production, the frontend is built and served by the backend.
#
# =============================================================================

# Backend API URL (for local dev with separate frontend/backend)
VITE_API_URL=http://localhost:8000

# Development auto-login (saves time during testing)
# Set to false in production!
VITE_DEV_AUTO_LOGIN=false
VITE_DEV_EMAIL=admin@localhost
VITE_DEV_PASSWORD=changeme
