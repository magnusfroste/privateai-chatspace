# RAG A/B Testing Configuration
# Copy to config.yaml and fill in your values

# Shared Services (both systems use these)
shared:
  llm:
    base_url: "https://api.autoversio.ai/v1"
    model: "autoversio"
    api_key: "sk-7eC6fR7qpxQOXCK2kYfPWg"
  embedder:
    base_url: "https://api.autoversio.ai/v1"
    model: "embed"
    api_key: "sk-7eC6fR7qpxQOXCK2kYfPWg"

# AnythingLLM Configuration
# Remote: https://chat.autoversio.ai (admin/12345678)
# Local:  http://localhost:3001
anythingllm:
  base_url: "https://chat.autoversio.ai"
  api_key: "M1X0NJ0-SZ8M65G-NT6KFQD-12JTTPD"  # Get from Settings → API Keys
  workspace_slug: "rag-test"

# Private AI Chatspace Configuration  
# Remote: https://your-deployment.com
# Local:  http://localhost:8000
privateai:
  base_url: "http://localhost:8000"
  # Login: admin@autoversio.local / changeme
  email: "admin@autoversio.local"
  password: "changeme"
  workspace_id: "2"  # rag-test workspace

# Evaluation Settings
evaluation:
  # Number of queries to run
  max_queries: 100
  
  # Retrieval settings
  top_k: 5
  
  # LLM-as-Judge settings
  judge_temperature: 0.1
  
  # Output
  results_dir: "./results"
  
  # Categories to test
  categories:
    - technical
    - legal
    - product
    - general
